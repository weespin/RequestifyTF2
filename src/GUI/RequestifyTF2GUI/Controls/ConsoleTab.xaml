<UserControl x:Class="RequestifyTF2GUI.Controls.ConsoleTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" Margin="0,0,0,30">
         <TextBlock FontSize="14" Grid.Row="0" x:Name="Console" 
        TextWrapping="Wrap"/>
        </ScrollViewer>
        <CheckBox Name="debugchk" Unchecked="Debugchk_OnUnchecked" Checked="ToggleButton_OnChecked" Content="Debug" Grid.Row="1"></CheckBox>
    </Grid>
</UserControl>
